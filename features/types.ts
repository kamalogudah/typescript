const today = new Date();

today.getTime();

const person = {
  age: 20,
};

person.age;

class Color {}

const red = new Color();

// Type annotation => code we add to tell typescript what type of value a varible will refer to
// Type inference => Typescript tries to figure out what type of value a variable referes to
